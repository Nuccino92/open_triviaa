const initialValue = {
  questions: [],
  mode: undefined,
};

const gameReducer = (state = initialValue, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default gameReducer;
